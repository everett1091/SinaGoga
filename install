echo "Введите пароль для установки SinaGoga v1.0:"
read -s USER_PASS

# Получаем реальный пароль с GitHub
realpass=$(curl -s https://raw.githubusercontent.com/everett1091/SinaGoga/main/auth.txt)

if [[ "$USER_PASS" != "$realpass" ]]; then
  echo "❌ Неверный пароль. Доступ запрещён."
  exit 1
fi

echo "✅ Пароль принят. Загружаем прошивку..."
wget -O sinagoga.tar.gz https://raw.githubusercontent.com/everett1091/SinaGoga/main/sinagoga.tar.gz

echo "📦 Распаковываем архив..."
tar -xzf sinagoga.tar.gz
cd sinagoga

echo "⚙️ Запускаем установку..."
chmod +x install.sh
./install.sh
