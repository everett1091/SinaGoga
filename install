echo "Введите пароль для установки SinaGoga v1.0:"
read -s USER_PASS
echo
echo "👉 Введённый пароль: [$USER_PASS]"

realpass=$(curl -s https://raw.githubusercontent.com/everett1091/SinaGoga/main/auth.txt | tr -d '\r\n')
echo "📦 Пароль с GitHub: [$realpass]"

if [[ "$USER_PASS" != "$realpass" ]]; then
  echo "❌ Неверный пароль. Доступ запрещён."
  exit 1
fi

echo "✅ Пароль принят. Загружаем прошивку..."
